{#- Glances v5 --fetch template: the TUI header, then its quicklook, MEM and LOAD blocks.
    `ui` draws TUI blocks (ui.block, ui.row, ui.rule, ui.title, ui.color, ui.top);
    `gl` is the Python API (e.g. gl.mem["percent"]). -#}
{{ ui.row('system', 'ip', 'uptime', gap=3) }}
{{ ui.rule() }}
{{ ui.row('quicklook', 'mem', 'load') }}
